# CUDA 环境专用依赖 (PaddleOCR GPU版本)
# 官方推荐安装命令：
# Python  环境
# 核心依赖：
paddlepaddle-gpu>=3.1.0
cupy-cuda12x>=12.0.0  # 自动匹配CUDA版本
cudatoolkit
cudnn
nvidia-cublas-cu12>=12.4.2.65
nvidia-cuda-nvrtc-cu12>=12.4.99
opencv-python-headless-cuda

# 可选加速库：
nccl
cub

# 注意：具体版本需匹配服务器CUDA驱动版本
# 验证安装：python -c "import paddle; paddle.utils.run_check()"