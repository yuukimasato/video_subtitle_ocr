# video-subtitle-ocr GPU requirements
# Same as CPU requirements, but with GPU-enabled PaddlePaddle variant.
# Requires: NVIDIA GPU + CUDA 12.x + cuDNN 9.x
PySide6>=6.5,<7.0
numpy>=1.24,<3.0
opencv-python>=4.8,<5.0
paddlepaddle-gpu>=3.1,<3.3
paddleocr>=2.10,<4.0
scikit-image>=0.22
Levenshtein>=0.25
