# video-subtitle-ocr GPU requirements
# Same as CPU requirements, but with GPU-enabled PaddlePaddle variant.
# Requires: NVIDIA GPU + CUDA 12.x + cuDNN 9.x
PySide6>=6.5,<7.0
numpy>=1.24,<3.0
# paddleocr>=3.7 -> paddlex[ocr] hard-pins opencv-contrib-python==4.10.0.84.
# Keep ONLY this one opencv package (never mix with opencv-python, ABI clash).
opencv-contrib-python==4.10.0.84
paddlepaddle-gpu>=3.2.1,<3.3
paddleocr>=3.7.0,<4.0
scikit-image>=0.22
Levenshtein>=0.25
openai>=1.0
tenacity>=8.0
json-repair>=0.7
# Optional CPU fallback engine (PP-OCRv6 ONNX models, no PaddlePaddle needed):
#   pip install "rapidocr>=3.9,<4.0"
